<Page
    x:Class="SmartHomeApplication.ClientUWP.View.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SmartHomeApplication.ClientUWP.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{StaticResource ViewColor}">

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Grid.Row="1" Margin="50,0,50,0">
            <Button Name="FaceBookButtonLogin" Margin="0,8,8,0" Command="{Binding LoginCommand}" Style="{StaticResource LoginButtonStyle}" Background="{StaticResource FacebookLoginButtonColor}">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="20,0,0,0">
                    <Image Source="{StaticResource FacebookLogo}" HorizontalAlignment="Left" Height="24" Width="24"/>
                    <TextBlock Text="Login with Facebook" Style="{StaticResource LoginButtonTextStyle}"/>
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</Page>
